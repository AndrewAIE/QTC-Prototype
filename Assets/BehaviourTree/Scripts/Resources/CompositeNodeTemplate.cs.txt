using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace BehaviourTree
{
    public class #SCRIPTNAME# : CompositeNode
    {
		//This is called once when the Behaviour Tree enters this node
		protected override void OnStart()
		{
			
		}
		//This is called once when the Behaviour Tree exits this node
		protected override void OnStop()
		{
			
		}
		//This is called once every frame while the Behaviour tree is accessing this node
		protected override State OnUpdate()
		{			
			return State.Success;
		}
		//This is called once every Fixed Timestep (default every 20ms)
		protected override void OnFixedUpdate()
        {
            
        }
    }
}
